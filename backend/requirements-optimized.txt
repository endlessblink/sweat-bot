# Optimized requirements.txt for Render deployment - CPU-only PyTorch + prebuilt wheels
# This reduces build time from 15+ minutes to 3-5 minutes and memory usage by 75%

# Core FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# AI Providers (no compilation required)
openai==1.3.0
groq==0.4.0
anthropic==0.7.0
google-generativeai==0.3.1

# Databases - CRITICAL: Use binary wheels to avoid compilation
asyncpg==0.29.0
psycopg2-binary==2.9.9  # Use binary version, NOT psycopg2
pymongo==4.6.0
redis==5.0.1

# PyTorch CPU-ONLY - This is the key to success!
# Must use --extra-index-url BEFORE torch installation
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
torchaudio==2.1.0+cpu

# Scientific packages - stable versions with reliable prebuilt wheels
numpy==1.24.3
scipy==1.10.1
librosa==0.10.1
soundfile==0.12.1

# Other optimized dependencies
phidata==2.3.0
httpx==0.25.0
aiohttp==3.9.1
websockets==12.0
structlog==23.2.0

# Configuration and utilities
python-dotenv==1.0.0
prometheus-client==0.19.0
pytest==7.4.3
pytest-asyncio==0.21.1

# Development
gunicorn==21.2.0