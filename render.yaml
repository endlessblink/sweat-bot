services:
  - type: web
    name: sweatbot-nodejs
    runtime: node
    plan: free
    env: node
    rootDir: backend-nodejs
    buildCommand: "npm install && npm run build"
    startCommand: "npm start"
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: sweatbot-db-nodejs
          property: connectionString

databases:
  - name: sweatbot-db-nodejs
    plan: free
    databaseName: sweatbot
    user: sweatbot